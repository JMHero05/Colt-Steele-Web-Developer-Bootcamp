<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Random</title>
</head>

<body>
	<h1>Your random number is: <%= randNum %></h1>
	<!-- <% if (randNum % 2 === 0) { %>
	<h2>That is an even number!</h2>
	<% } else { %>
	<h2>That is an odd number!</h2>
	<% } %> -->
	<% let numType = '' %>
	<% randNum % 2 === 0 ? numType = 'even' : numType = 'odd' %>
	<h2>That is an <%= numType %> number!</h2>

	<h2><%= randNum % 2 === 0 ? 'That is an even number!' : 'That is an odd number!'%></h2>
</body>

</html>